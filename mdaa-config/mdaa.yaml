organization: "{{env_var:MDAA_ORG}}"

domains:
  mlops:
    environments:
      dev:
        account: default
        use_bootstrap: false
        modules:
          pipeline:
            module_path: "@aws-mdaa/sagemaker-pipeline"
            module_configs:
              - ./pipeline.yaml
